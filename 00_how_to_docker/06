# we can to download nginx container from Docker Hub, as a background task.

docker run --name overlord --restart=always -d -p 5000:80 nginx

# -d, --detach — Run container in background and print container ID.
# --name string — Assign a name to the container.
# -p, --publish list — Publish a container's port(s) to the host.
# always - Always restart the container regardless of the exit status.
# Check     $ docker image ls
# Nginx — web server and mail proxy server running on Unix-like operating systems.