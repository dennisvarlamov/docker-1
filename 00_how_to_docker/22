docker-machine ssh Aiur "docker swarm join --token $(docker swarm join-token worker -q) $(docker-machine ip Char):2376"

# Log into or run a command on a machine using SSH
# To login, you can tap the command 
#   docker-machine ssh machinename